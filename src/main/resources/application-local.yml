spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5438/files
    username: db
    password: db
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8082/.well-known/jwks.json
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8089

byz:
  kafka:
    enabled: true
